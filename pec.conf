# --- OPÇÃO A: acesso só por IP da VPS (sem domínio ainda) ---
server {
    listen 80;
    server_name 179.197.71.42;

    location / {
        proxy_pass http://pec:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 50M;
    }
}

# --- OPÇÃO B: quando decidir o domínio, descomente e ajuste ---
# server {
#     listen 80;
#     server_name pec.seudominio.com.br;
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl;
#     server_name pec.seudominio.com.br;
#
#     ssl_certificate     /etc/nginx/certs/live/pec.seudominio.com.br/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/live/pec.seudominio.com.br/privkey.pem;
#
#     location / {
#         proxy_pass http://pec:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         client_max_body_size 50M;
#     }
# }
